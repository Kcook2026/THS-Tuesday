{
  "name": "User",
  "type": "object",
  "properties": {
    "role": {
      "type": "string",
      "description": "The role of the user in the app",
      "enum": [
        "admin",
        "manager",
        "user",
        "read_only"
      ],
      "default": "user"
    }
  },
  "required": [
    "role"
  ]
}